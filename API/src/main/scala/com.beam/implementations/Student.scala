//import Person._
import parents.Human._

package Student{
  class Student
}
class Student extends Human{
   def test(): Boolean ={
    true
  }
  def isAlive(x: Any): Boolean ={
    true
  }
  def computeAge: Int ={
    5
  }
}
